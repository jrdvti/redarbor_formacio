# Obrim terminal
# Obrim Docker Desktop
# Anem a la carpeta on s'ha guardat el docker-compose.yml i executem

docker-compose up -d

# Mirem que al Docker Deskotp estigui el contenidor
# Esperem una estona
# Fem click on surt

http://localhost:5601
